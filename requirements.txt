# CAIRN Evaluation Framework – Python Dependencies
#
# Installed in a venv that MUST match the Python version linked
# by SWI-Prolog's Janus.  setup.sh detects this automatically.

# Graph analysis (directed + undirected topology, centrality, and
# clique-complex Betti numbers via mod-2 boundary-matrix rank in
# tools/utils.py -- no separate homology library needed/used)
networkx>=3.0

# Scientific computing (Pearson correlation, Shannon entropy, arrays)
numpy>=1.20,<2.0
scipy>=1.7,<1.12

# --- Live dashboard (read-only consumer of tools/recorder.py output) ---
# Confirmed installable in the Python 3.9.6 venv (streamlit 1.50.0 as of writing).
streamlit>=1.33,<2.0   # UI framework; st.fragment(run_every=...) drives live auto-refresh
pandas>=1.5,<3.0       # CSV parsing + na_values=['N/A'] coercion
plotly>=5.18,<6.0      # independent per-panel y-axes / optional log scale
